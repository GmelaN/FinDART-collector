[project]
name = "findart-collector"
version = "0.1.0"
description = "Collectors for FinDART source documents"
requires-python = ">=3.11"
dependencies = ["requests>=2.31"]

[project.scripts]
findart-policy-briefing = "findart_collector.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

